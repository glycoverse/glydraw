% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-glycan.R
\name{guide_glycan}
\alias{guide_glycan}
\title{Use glycan cartoons as legend labels}
\usage{
guide_glycan(
  title = ggplot2::waiver(),
  theme = NULL,
  position = NULL,
  direction = NULL,
  override.aes = list(),
  nrow = NULL,
  ncol = NULL,
  reverse = FALSE,
  order = 0,
  size = 0.4,
  orient = c("H", "V"),
  hjust = 0,
  vjust = vjust_red_end(),
  show_linkage = TRUE,
  red_end = "",
  fuc_orient = c("flex", "up"),
  edge_linewidth = 0.8,
  node_linewidth = 0.8,
  node_size = 1,
  colors = NULL,
  style = NULL
)
}
\arguments{
\item{title}{A character string or expression indicating a title of guide.
If \code{NULL}, the title is not shown. By default
(\code{\link[ggplot2:waiver]{waiver()}}), the name of the scale object or the name
specified in \code{\link[ggplot2:labs]{labs()}} is used for the title.}

\item{theme}{A \code{\link[ggplot2:theme]{theme}} object to style the guide individually or
differently from the plot's theme settings. The \code{theme} argument in the
guide partially overrides, and is combined with, the plot's theme.
Arguments that apply to a single legend are respected, most of which have
the \code{legend}-prefix. Arguments that apply to combined legends
(the legend box) are ignored, including \code{legend.position},
\verb{legend.justification.*}, \code{legend.location} and \verb{legend.box.*}.}

\item{position}{A character string indicating where the legend should be
placed relative to the plot panels.
One of "top", "right", "bottom", "left", or "inside".}

\item{direction}{A character string indicating the direction of the guide.
One of "horizontal" or "vertical".}

\item{override.aes}{A list specifying aesthetic parameters of legend key.
See details and examples.}

\item{nrow, ncol}{The desired number of rows and column of legends
respectively.}

\item{reverse}{logical. If \code{TRUE} the order of legends is reversed.}

\item{order}{positive integer less than 99 that specifies the order of
this guide among multiple guides. This controls the order in which
multiple guides are displayed, not the contents of the guide itself.
If 0 (default), the order is determined by a secret algorithm.}

\item{size}{Positive scalar that uniformly scales each legend-label cartoon.
Defaults to \code{0.4}.}

\item{orient}{Glycan drawing orientation, either \code{"H"} for horizontal or
\code{"V"} for vertical. Defaults to \code{"H"}.}

\item{hjust}{Horizontal cartoon justification between \code{0} and \code{1}, or
\code{\link[=hjust_red_end]{hjust_red_end()}}. It defaults to \code{0} for horizontal cartoons and
\code{\link[=hjust_red_end]{hjust_red_end()}} for vertical cartoons.}

\item{vjust}{Vertical cartoon justification between \code{0} and \code{1}, or
\code{\link[=vjust_red_end]{vjust_red_end()}}. It defaults to \code{\link[=vjust_red_end]{vjust_red_end()}} for horizontal
cartoons and \code{0.5} for vertical cartoons.}

\item{show_linkage}{Whether to show glycosidic linkage annotations inside
the cartoons. Defaults to \code{TRUE}.}

\item{red_end}{Reducing-end annotation passed to \code{\link[=glycanGrob]{glycanGrob()}}. Use \code{"~"}
for a wave, or another string to display that text. Defaults to \code{""}.}

\item{fuc_orient}{Fuc-like triangle orientation passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{edge_linewidth}{Linkage linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_linewidth}{Node-border linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_size}{Node-size multiplier passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{colors}{Optional named character vector of monosaccharide fill colors
passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{style}{A \code{glydraw_style} object that supplies rendering options.
Explicitly supplied rendering arguments override it.}
}
\value{
A ggplot2 legend guide that draws glycan cartoons in place of text
labels.
}
\description{
\code{guide_glycan()} is a legend guide that replaces the usual text labels with
glycan cartoons while retaining the legend keys drawn by the plot layers.
Mapped discrete values and values returned by the scale's \code{labels} argument
may be glycan structure strings supported by \code{\link[glyparse:auto_parse]{glyparse::auto_parse()}} or
\code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vectors.
}
\examples{
glycans <- data.frame(
  structure = c(
    "Gal(b1-3)GalNAc(a1-",
    "Man(a1-3)[Man(a1-6)]Man(b1-4)GlcNAc(b1-"
  ),
  abundance = c(12, 8)
)

ggplot2::ggplot(
  glycans,
  ggplot2::aes(
    x = .data$structure,
    y = .data$abundance,
    fill = .data$structure
  )
) +
  ggplot2::geom_col() +
  ggplot2::scale_fill_discrete(guide = guide_glycan())
}
