% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-glycan.R
\name{geom_glycan}
\alias{geom_glycan}
\title{Draw glycans at ggplot2 positions}
\usage{
geom_glycan(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  angle = 0,
  show_linkage = TRUE,
  orient = c("H", "V"),
  fuc_orient = c("flex", "up"),
  red_end = "",
  edge_linewidth = 0.8,
  node_linewidth = 0.8,
  node_size = 1,
  colors = NULL,
  highlight = NULL,
  style = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}. The
\code{x}, \code{y}, and \code{structure} aesthetics are required.}

\item{data}{The data to be displayed in this layer. When \code{NULL}, the default,
the data is inherited from the plot data.}

\item{stat}{The statistical transformation to use on the data for this
layer. Defaults to \code{"identity"}.}

\item{position}{A position adjustment to use on the data for this layer.
Defaults to \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{angle}{Rotation in degrees. Like \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}, this can be
supplied as an aesthetic or a fixed layer value. It rotates each completed
cartoon around its mapped position independently of \code{orient}. Defaults to
\code{0}.}

\item{show_linkage}{Show glycosidic linkage annotations or not. Default is
TRUE. Substituent annotations are always shown.}

\item{orient}{The orientation of glycan structure. "H" for horizontal, "V" for vertical.
Default is "H"}

\item{fuc_orient}{Fuc-like triangle orientation. \code{"flex"} points
non-reducing Fuc-like residues toward their rendered linkage direction,
while \code{"up"} draws all Fuc-like triangles pointing upward. Reducing-end
Fuc-like residues always point upward. Defaults to \code{"flex"}.}

\item{red_end}{Reducing-end annotation. The default \code{""} keeps the current
reducing-end line. Use \code{"~"} to add a wavy reducing end, or any other
string to draw that string at the reducing end.}

\item{edge_linewidth}{Numeric scalar controlling the linewidth of linkage
lines. Defaults to the current value, \code{0.8}.}

\item{node_linewidth}{Numeric scalar controlling the linewidth of node
borders. Defaults to the current value, \code{0.8}.}

\item{node_size}{Numeric scalar used as a multiplier for the default node
size. Defaults to \code{1}, which keeps the current size. Linkage annotations
are moved farther from larger nodes, and are hidden with a warning when
\code{node_size} is too large to leave enough annotation space. Values larger
than \code{2} are rejected because residues overlap.}

\item{colors}{Optional named character vector of custom monosaccharide fill
colors. Names must be supported monosaccharide names, such as \code{"Gal"} or
\code{"GlcNAc"}. User-provided colors overwrite the default SNFG colors, while
unprovided monosaccharides keep their default colors. Defaults to \code{NULL}.}

\item{highlight}{An integer vector specifying the node indices to highlight.
This argument is applicable only when \code{structure} is a \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}}.
Note that for a \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}}, the node indices correspond exactly
to the monosaccharides in its printed IUPAC nomenclature.
For example, given \code{glyrepr::as_glycan_structure("Gal(b1-3)[GlcNAc(b1-6)]GalNAc(a1-")},
setting \code{highlight = c(1, 3)} will highlight the "Gal" and "GalNAc" nodes.}

\item{style}{A \code{glydraw_style} object that supplies rendering options.
Explicitly supplied rendering arguments override it.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}
}
\value{
A ggplot2 layer that can be added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
\code{geom_glycan()} draws one glycan cartoon for each data row. Each cartoon is
anchored at its mapped \code{x} and \code{y} position and retains the structure-derived
dimensions and appearance used by \code{\link[=draw_cartoon]{draw_cartoon()}}. The optional \code{size}
aesthetic scales the complete cartoon uniformly, including nodes, lines,
text, and spacing, without changing their relative appearance. Like points
and text, the cartoons do not expand the position scales beyond their anchor
coordinates. Use scale expansion or explicit coordinate limits when the
cartoons need more room around the panel edges. Unlike standalone cartoons
returned by \code{\link[=draw_cartoon]{draw_cartoon()}}, cartoons in this layer have no output border or
background.
}
\section{Aesthetics}{

\code{geom_glycan()} understands the following aesthetics. \code{x}, \code{y}, and
\code{structure} are required; the remaining aesthetics are optional:
\itemize{
\item \code{x}
\item \code{y}
\item \code{structure}, containing glycan structure strings or
\code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} values
\item \code{size}, an optional whole-cartoon scale multiplier that defaults to \code{1}.
Mapped values are transformed by ggplot2's size scale; use
\code{\link[ggplot2:scale_identity]{ggplot2::scale_size_identity()}} when the mapped values are literal
multipliers. This is distinct from \code{node_size}, which changes residue size
within the cartoon.
\item \code{hjust}, an optional horizontal justification that defaults to \code{0.5}.
Numeric \code{0} aligns the cartoon content's left bound with \code{x}, and \code{1}
aligns its right bound. Use \code{\link[=hjust_red_end]{hjust_red_end()}} to anchor a vertical cartoon
at its reducing end.
\item \code{vjust}, an optional vertical justification that defaults to \code{0.5}.
Numeric \code{0} aligns the cartoon content's bottom bound with \code{y}, including
the end of a reducing-end annotation line, and \code{1} aligns its top bound.
Use \code{\link[=vjust_red_end]{vjust_red_end()}} to anchor a horizontal cartoon at its reducing end.
\item \code{angle}, an optional rotation in degrees that defaults to \code{0}. Rotation is
applied after the cartoon is drawn, independently of \code{orient}.
}
}

\examples{
glycans <- data.frame(
  x = c(1, 3),
  y = c(1, 2),
  size = c(0.7, 1.1),
  structure = c(
    "Gal(b1-3)GalNAc(a1-",
    "Man(a1-3)[Man(a1-6)]Man(b1-4)GlcNAc(b1-"
  )
)

ggplot2::ggplot(
  glycans,
  ggplot2::aes(
    x = .data$x,
    y = .data$y,
    structure = .data$structure,
    size = .data$size
  )
) +
  geom_glycan() +
  ggplot2::scale_size_identity() +
  ggplot2::coord_cartesian(
    xlim = c(0, 4),
    ylim = c(0, 3),
    expand = FALSE
  )

# Bottom-align a row of vertical glycans with different heights.
ggplot2::ggplot(
  glycans,
  ggplot2::aes(x = .data$x, y = 1, structure = .data$structure)
) +
  geom_glycan(orient = "V", vjust = 0)
}
