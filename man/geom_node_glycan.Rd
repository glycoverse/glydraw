% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-node-glycan.R
\name{geom_node_glycan}
\alias{geom_node_glycan}
\title{Draw glycans as ggraph nodes}
\usage{
geom_node_glycan(
  mapping = NULL,
  data = NULL,
  position = "identity",
  show.legend = NA,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}. The
\code{structure} aesthetic is required. The \code{x} and \code{y} aesthetics default to
the node coordinates calculated by ggraph.}

\item{data}{The node data to display. When \code{NULL}, the default, the layer
uses the node data calculated by \code{\link[ggraph:ggraph]{ggraph::ggraph()}}.}

\item{position}{A position adjustment. Defaults to \code{"identity"}.}

\item{show.legend}{Logical. Should this layer be included in legends?}

\item{...}{Arguments passed to \code{\link[=geom_glycan]{geom_glycan()}}, including fixed aesthetics,
drawing controls, and \code{style}.}
}
\value{
A ggplot2 layer that can be added to a \code{\link[ggraph:ggraph]{ggraph::ggraph()}} plot.
}
\description{
\code{geom_node_glycan()} is a \link[ggraph:ggraph]{ggraph} node layer backed by
\code{\link[=geom_glycan]{geom_glycan()}}. It supplies the node layout's \code{x} and \code{y} columns as the
default position aesthetics and supports ggraph's \code{filter} aesthetic.
}
\section{Aesthetics}{

\code{geom_node_glycan()} understands the same aesthetics as \code{\link[=geom_glycan]{geom_glycan()}}.
\code{structure} is required, while \code{x} and \code{y} default to the node layout
coordinates. It additionally supports \code{filter}, a logical aesthetic that
selects which nodes are drawn.
}

\examples{
if (requireNamespace("ggraph", quietly = TRUE)) {
  graph <- igraph::make_ring(2)
  igraph::V(graph)$glycan <- c(
    "GalNAc(a1-",
    "Gal(b1-3)GalNAc(a1-"
  )

  ggraph::ggraph(graph, layout = "linear") +
    ggraph::geom_edge_link() +
    geom_node_glycan(ggplot2::aes(structure = .data$glycan))
}
}
