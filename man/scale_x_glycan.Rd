% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-glycan.R
\name{scale_x_glycan}
\alias{scale_x_glycan}
\alias{scale_y_glycan}
\title{Use glycan cartoons as axis labels}
\usage{
scale_x_glycan(
  name = ggplot2::waiver(),
  ...,
  palette = seq_len,
  expand = ggplot2::waiver(),
  position = "bottom",
  sec.axis = ggplot2::waiver(),
  continuous.limits = NULL,
  size = 0.4,
  angle = 0,
  hjust = hjust_red_end(),
  vjust = 0,
  nudge_x = 0,
  nudge_y = 0,
  show_linkage = TRUE,
  red_end = "",
  fuc_orient = c("flex", "up"),
  edge_linewidth = 0.8,
  node_linewidth = 0.8,
  node_size = 1,
  colors = NULL,
  style = NULL
)

scale_y_glycan(
  name = ggplot2::waiver(),
  ...,
  palette = seq_len,
  expand = ggplot2::waiver(),
  position = "left",
  sec.axis = ggplot2::waiver(),
  continuous.limits = NULL,
  size = 0.4,
  angle = 0,
  hjust = 1,
  vjust = vjust_red_end(),
  nudge_x = 0,
  nudge_y = 0,
  show_linkage = TRUE,
  red_end = "",
  fuc_orient = c("flex", "up"),
  edge_linewidth = 0.8,
  node_linewidth = 0.8,
  node_size = 1,
  colors = NULL,
  style = NULL
)
}
\arguments{
\item{name}{The name of the scale, displayed as the axis title. Use \code{NULL}
to remove the title.}

\item{...}{Arguments passed to \code{\link[ggplot2:scale_discrete]{ggplot2::scale_x_discrete()}} or
\code{\link[ggplot2:scale_discrete]{ggplot2::scale_y_discrete()}}, including \code{breaks} and \code{labels}.}

\item{palette}{A palette function that returns discrete position values.}

\item{expand}{Expansion applied to the discrete position scale.}

\item{position}{Position of the axis.}

\item{sec.axis}{A secondary axis specification.}

\item{continuous.limits}{Continuous limits used to position the discrete
scale.}

\item{size}{Positive scalar that uniformly scales each axis-label cartoon.
Defaults to \code{0.4}.}

\item{angle}{Rotation in degrees applied to each axis-label cartoon,
independently of the cartoon orientation. Defaults to \code{0}.}

\item{hjust}{Horizontal justification. Vertical x-axis cartoons default to
\code{\link[=hjust_red_end]{hjust_red_end()}}, while horizontal y-axis cartoons default to \code{1}.}

\item{vjust}{Vertical justification. Vertical x-axis cartoons default to
\code{0}, while horizontal y-axis cartoons default to \code{\link[=vjust_red_end]{vjust_red_end()}}.}

\item{nudge_x}{Horizontal adjustment of each cartoon, in millimetres.
Positive values move cartoons to the right. When this moves cartoons
toward or away from a y-axis title, the title moves with them to preserve
the gap. Defaults to \code{0}.}

\item{nudge_y}{Vertical adjustment of each cartoon, in millimetres. Positive
values move cartoons upward. When this moves cartoons toward or away from
an x-axis title, the title moves with them to preserve the gap. Defaults
to \code{0}.}

\item{show_linkage}{Whether to show glycosidic linkage annotations inside
the cartoons. Defaults to \code{TRUE}.}

\item{red_end}{Reducing-end annotation passed to \code{\link[=glycanGrob]{glycanGrob()}}. Use \code{"~"}
for a wave, or another string to display that text. Defaults to \code{""}.}

\item{fuc_orient}{Fuc-like triangle orientation passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{edge_linewidth}{Linkage linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_linewidth}{Node-border linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_size}{Node-size multiplier passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{colors}{Optional named character vector of monosaccharide fill colors
passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{style}{A \code{glydraw_style} object that supplies rendering options. Its
orientation is ignored because each scale determines the axis direction.
Explicitly supplied rendering arguments override it.}
}
\value{
A ggplot2 discrete position scale.
}
\description{
\code{scale_x_glycan()} and \code{scale_y_glycan()} are discrete position scales that
replace text tick labels with compact glycan cartoons. Mapped discrete values
and values returned by the scale's \code{labels} argument may be glycan structure
strings supported by \code{\link[glyparse:auto_parse]{glyparse::auto_parse()}} or
\code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vectors. X-axis cartoons are vertical and
bottom-aligned by default, while y-axis cartoons are horizontal and
right-aligned by default. The cartoon orientation and alignment adapt to the
displayed axis, including when the axes are swapped by
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}.
}
\examples{
glycans <- data.frame(
  structure = c(
    "Gal(b1-3)GalNAc(a1-",
    "Man(a1-3)[Man(a1-6)]Man(b1-4)GlcNAc(b1-"
  ),
  abundance = c(12, 8)
)

ggplot2::ggplot(glycans, ggplot2::aes(x = structure, y = abundance)) +
  ggplot2::geom_col() +
  scale_x_glycan()
}
