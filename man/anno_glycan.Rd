% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anno-glycan.R
\name{anno_glycan}
\alias{anno_glycan}
\title{Use glycan cartoons as ComplexHeatmap labels}
\usage{
anno_glycan(
  structure,
  which = c("column", "row"),
  side = NULL,
  size = 0.4,
  angle = 0,
  hjust = NULL,
  vjust = NULL,
  nudge_x = 0,
  nudge_y = 0,
  show_linkage = TRUE,
  red_end = "",
  fuc_orient = c("flex", "up"),
  edge_linewidth = 0.8,
  node_linewidth = 0.8,
  node_size = 1,
  colors = NULL,
  font_family = "",
  style = NULL,
  width = NULL,
  height = NULL,
  show_name = FALSE
)
}
\arguments{
\item{structure}{A character vector of glycan structure strings supported by
\code{\link[glyparse:auto_parse]{glyparse::auto_parse()}} or a \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vector. Its
order must match the rows or columns of the heatmap matrix.}

\item{which}{Whether the cartoons label heatmap \code{"column"} or \code{"row"}
observations.}

\item{side}{Side on which the annotation is placed. Column annotations
accept \code{"bottom"} or \code{"top"}; row annotations accept \code{"left"} or
\code{"right"}. Defaults to the corresponding glycan scale position.}

\item{size}{Positive scalar that uniformly scales each cartoon. Defaults to
\code{0.4}.}

\item{angle}{Rotation in degrees applied to each cartoon independently of
its drawing orientation. Defaults to \code{0}.}

\item{hjust}{Horizontal justification. \code{NULL} uses \code{\link[=hjust_red_end]{hjust_red_end()}} for
column labels and \code{1} for row labels.}

\item{vjust}{Vertical justification. \code{NULL} uses \code{0} for column labels and
\code{\link[=vjust_red_end]{vjust_red_end()}} for row labels.}

\item{nudge_x}{Horizontal adjustment of each cartoon, in millimetres.
Positive values move cartoons to the right. Defaults to \code{0}.}

\item{nudge_y}{Vertical adjustment of each cartoon, in millimetres. Positive
values move cartoons upward. Defaults to \code{0}.}

\item{show_linkage}{Whether to show glycosidic linkage annotations inside
the cartoons. Defaults to \code{TRUE}.}

\item{red_end}{Reducing-end annotation passed to \code{\link[=glycanGrob]{glycanGrob()}}. Use \code{"~"}
for a wave, or another string to display that text. Defaults to \code{""}.}

\item{fuc_orient}{Fuc-like triangle orientation passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{edge_linewidth}{Linkage linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_linewidth}{Node-border linewidth passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{node_size}{Node-size multiplier passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{colors}{Optional named character vector of monosaccharide fill colors
passed to \code{\link[=glycanGrob]{glycanGrob()}}.}

\item{font_family}{Font family used for text annotations.}

\item{style}{A \code{glydraw_style} object that supplies rendering options. Its
orientation is ignored because \code{which} determines the label direction.
Explicitly supplied rendering arguments override it.}

\item{width}{Optional \code{\link[grid:unit]{grid::unit()}} width for a row annotation. \code{NULL}
calculates the width from the rendered cartoons.}

\item{height}{Optional \code{\link[grid:unit]{grid::unit()}} height for a column annotation. \code{NULL}
calculates the height from the rendered cartoons.}

\item{show_name}{Whether ComplexHeatmap should show the annotation name.
Defaults to \code{FALSE} because the cartoons normally replace row or column
names.}
}
\value{
A ComplexHeatmap \code{AnnotationFunction} object.
}
\description{
\code{anno_glycan()} creates a \code{\link[ComplexHeatmap:AnnotationFunction]{ComplexHeatmap::AnnotationFunction()}} that draws
glycan cartoons in the order used by a ComplexHeatmap heatmap. The cartoons
follow row or column clustering, reordering, and splitting.
}
\details{
Column annotations use the visual defaults of \code{\link[=scale_x_glycan]{scale_x_glycan()}}: vertical
cartoons anchored at their reducing ends and aligned along their bottom
bounds. Row annotations use the defaults of \code{\link[=scale_y_glycan]{scale_y_glycan()}}: horizontal
cartoons aligned along their right bounds and anchored at their reducing
ends. \code{side} should match the heatmap annotation side and defaults to
\code{"bottom"} for columns and \code{"left"} for rows.

The required annotation width or height is calculated from the largest
rendered cartoon, including rotation and perpendicular nudging. Supply
\code{width} for row annotations or \code{height} for column annotations to override
the calculated size.
}
\examples{
if (requireNamespace("ComplexHeatmap", quietly = TRUE)) {
  mat <- matrix(
    seq_len(9),
    nrow = 3,
    dimnames = list(paste0("row", 1:3), paste0("column", 1:3))
  )
  structures <- c(
    "GlcNAc(??-",
    "Gal(??-?)GlcNAc(??-",
    "Neu5Ac(??-?)Gal(??-?)GlcNAc(??-"
  )

  ComplexHeatmap::Heatmap(
    mat,
    show_row_names = FALSE,
    show_column_names = FALSE,
    left_annotation = ComplexHeatmap::rowAnnotation(
      glycan = anno_glycan(structures, which = "row")
    ),
    bottom_annotation = ComplexHeatmap::HeatmapAnnotation(
      glycan = anno_glycan(structures, which = "column")
    )
  )
}
}
